
////////////////////
// Types
////////////////////

import { IAccountDerived } from 'state/vault/types';


////////////////////
// Util Functions
////////////////////

/**
 * Verifies if the list of accounts are whitelisted
 * @param accounts Accounts that will be verified.
 * @param dappAccounts Whitelist that will check be checked against;
 * @returns An array of the dapp white listed accounts
 */
export const isAccountDappWhiteListed = (url: string, walletAccounts: string[], dappAccounts: string[]): string[] => {

    

}