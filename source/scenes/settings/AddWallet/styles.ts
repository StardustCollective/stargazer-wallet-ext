import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 16,
    flexGrow: 1,
  },
  contentContainer: {
    flexGrow: 1,
  },
});

export default styles;
