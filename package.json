{
  "name": "stargazer-wallet-ext",
  "version": "2.1.1",
  "description": "Extension wallet for $DAG tokens.",
  "private": true,
  "repository": "https://github.com/StardustCollective/stargazer-wallet-ext.git",
  "license": "MIT",
  "engines": {
    "node": ">=10.0.0",
    "yarn": ">= 1.0.0"
  },
  "scripts": {
    "dev:chrome": "yarn workspace extension cross-env NODE_ENV=development cross-env TARGET_BROWSER=chrome webpack --watch",
    "dev:firefox": "yarn workspace extension cross-env NODE_ENV=development cross-env TARGET_BROWSER=firefox webpack --watch",
    "build:chrome": "yarn workspace extension cross-env NODE_ENV=production cross-env TARGET_BROWSER=chrome webpack",
    "build:firefox": "yarn workspace extension cross-env NODE_ENV=production cross-env TARGET_BROWSER=firefox webpack",
    "build": "yarn workspace extension && yarn run build:chrome && yarn run build:firefox",
    "android": "yarn workspace mobile react-native run-android",
    "ios": "yarn workspace mobile react-native run-ios",
    "start": "yarn workspace mobile react-native start",
    "lint": "eslint . --ext .ts,.tsx",
    "lint:fix": "eslint . --ext .ts,.tsx --fix",
    "postinstall": "npx patch-package"
  },
  "dependencies": {
  },
  "devDependencies": {
  },
  "workspaces": ["source/web", "source/native"]
}
